//variables for story events/flags/etc



checkStoryTriggers = function () {
  if (cam.position.x < -2900 && cam.position.z > 1000) {
    const key = "<span id='key'>Key</span>";
    $("#credits p").text(`There is a ${key} here.`);
  }
  else if (cam.position.x > 3000) {
    console.log("X > 3000");
  }
  else if (cam.position.x > 3000) {
    console.log("X > 3000");
  }
  else if (cam.position.x > 3000) {
    console.log("X > 3000");
  }
  else if (cam.position.x > 3000) {
    console.log("X > 3000");
  }
  else if (cam.position.x > 3000) {
    console.log("X > 3000");
  }
  else if (cam.position.x > 3000) {
    console.log("X > 3000");
  }
  else if (cam.position.x > 3000) {
    console.log("X > 3000");
  }
  else if (cam.position.x > 3000) {
    console.log("X > 3000");
  }
  else if (cam.position.x > 3000) {
    console.log("X > 3000");
  }
  else if (cam.position.x > 3000) {
    console.log("X > 3000");
  }
}